# Global sieve: route Exim-sent copies into the Sent mailbox.
sieve_script save_sent_before {
  type = before
  path = /etc/dovecot/sieve/save-to-sent.sieve
}

protocol lmtp {
  mail_plugins {
    sieve = yes
  }
}
