# Exim 4 configuration — runtime values rendered from /etc/exim4/dynamic/ at startup
# Placeholders: @@PRIMARY_HOSTNAME@@ @@QUALIFY_DOMAIN@@ @@DKIM_SELECTOR@@

primary_hostname = @@PRIMARY_HOSTNAME@@
domainlist local_domains = /etc/exim4/dynamic/domains
domainlist relay_to_domains =
hostlist relay_from_hosts = 127.0.0.1
qualify_domain = @@QUALIFY_DOMAIN@@

# Prefer IPv4; disable IPv6 to avoid unreachable Gmail MX AAAA records
dns_ipv4_lookup = true
disable_ipv6 = true

# TLS
tls_advertise_hosts = *
tls_certificate = /etc/exim4/ssl/fullchain.pem
tls_privatekey = /etc/exim4/ssl/privkey.pem

# Listen on multiple ports
# Port 465 uses implicit TLS (SMTPS), ports 25 and 587 use STARTTLS
daemon_smtp_ports = 25 : 587
tls_on_connect_ports = 465

# ACL
acl_smtp_mail = acl_check_mail
acl_smtp_rcpt = acl_check_rcpt

# Main section
log_selector = +all
never_users = root
keep_environment =

# Authentication
auth_advertise_hosts = *
acl_smtp_auth = acl_check_auth

# Always trust local/Docker (Roundcube webmail submits from the Docker network).
hostlist smtp_local_hosts = <; 127.0.0.1 ; ::1 ; 172.16.0.0/12 ; 10.0.0.0/8

# Begin ACL
begin acl

# Global mode (Mail Admin → Domains → SMTP policy):
#   _mode:any            → AUTH from anywhere; per-domain IP lists apply only when set
#   _mode:allowlist_only → AUTH only from Docker/webmail + union of all domain allowlist IPs
# NOTE: Exim ${lookup{k}lsearch{f}{X}} → X on SUCCESS (not default-on-miss).
# Use {$value}{default} to read the file value with a miss fallback.
acl_check_auth:
  accept  hosts = +smtp_local_hosts
  accept  condition = ${if eq{${lookup{_mode}lsearch{/etc/exim4/dynamic/smtp_allow}{$value}{any}}}{any}}
  accept  hosts = ${lookup{_all}lsearch{/etc/exim4/dynamic/smtp_allow}{$value}{127.0.0.1}}
  deny    message = SMTP authentication is restricted to allowlisted IPs (global SMTP policy)

# Reject spoofed local senders and enforce From = authenticated account.
acl_check_mail:
  accept  senders = :
  accept  hosts = +relay_from_hosts
  deny    message = Local senders must authenticate before sending mail.
          sender_domains = +local_domains
          !authenticated = *
  # Global allowlist-only: only Docker/webmail + that domain's IPs may send (empty domain list = webmail only)
  deny    message = SMTP sending from your IP is not allowed (global policy: allowlisted IPs only). Add the IP under Domains → SMTP allow IPs.
          authenticated = *
          condition = ${if eq{${lookup{_mode}lsearch{/etc/exim4/dynamic/smtp_allow}{$value}{any}}}{allowlist_only}}
          !hosts = +smtp_local_hosts : ${lookup{${lc:${sg{$authenticated_id}{\N^.*@\N}{}}}}lsearch{/etc/exim4/dynamic/smtp_allow}{$value}{}}
  # Mode "any": enforce per-domain list only when that domain has IPs configured
  deny    message = SMTP sending from your IP is not allowed for this domain. Add the IP under Domains → SMTP allow IPs.
          authenticated = *
          condition = ${if eq{${lookup{_mode}lsearch{/etc/exim4/dynamic/smtp_allow}{$value}{any}}}{any}}
          condition = ${lookup{${lc:${sg{$authenticated_id}{\N^.*@\N}{}}}}lsearch{/etc/exim4/dynamic/smtp_allow}{yes}{no}}
          !hosts = +smtp_local_hosts : ${lookup{${lc:${sg{$authenticated_id}{\N^.*@\N}{}}}}lsearch{/etc/exim4/dynamic/smtp_allow}{$value}{}}
  # From matches authenticated account
  accept  authenticated = *
          condition = ${if eq{${lc:$sender_address}}{${lc:$authenticated_id}}{yes}{no}}
  # From is a permitted send alias for this authenticated account (see send_aliases in admin panel)
  accept  authenticated = *
          condition = ${if eq{${lc:$authenticated_id}}{${lookup{${lc:$sender_address}}lsearch{/etc/exim4/dynamic/send_aliases}}}{yes}{no}}
  deny    message = Sender address must match your authenticated account or a permitted send alias.
          authenticated = *

acl_check_rcpt:
  accept  hosts = :
  deny    message = Restricted characters in address
          domains = +local_domains
          local_parts = ^[.] : ^.*[@%!/|]
  accept  local_parts = postmaster
          domains = +local_domains
  accept  hosts = +relay_from_hosts

  # Per-account outbound rate limits (configured via admin panel)
  # Lookups must run on warn lines: ratelimit splits on "/" so paths like /etc/exim4/... break inline lookups.
  # NOTE: ${lookup{k}lsearch{f}{X}} uses X on SUCCESS — use {$value}{default} for file value + miss fallback.
  warn    set acl_c0 = ${if def:authenticated_id{$authenticated_id}{$sender_address}}
          set acl_m0 = ${lookup{$acl_c0}lsearch{/etc/exim4/dynamic/rate_10m}{$value}{50}}
          set acl_m1 = ${lookup{$acl_c0}lsearch{/etc/exim4/dynamic/rate_1h}{$value}{200}}
          set acl_m2 = ${lookup{$acl_c0}lsearch{/etc/exim4/dynamic/rate_1d}{$value}{1000}}
          condition = ${lookup{$acl_c0}lsearch{/etc/exim4/passwd}{yes}{no}}
  deny    message = Rate limit exceeded: too many messages in 10 minutes. Try again later.
          condition = ${lookup{$acl_c0}lsearch{/etc/exim4/passwd}{yes}{no}}
          ratelimit = $acl_m0 / 10m / per_rcpt / strict / $acl_c0
  deny    message = Rate limit exceeded: too many messages in 1 hour. Try again later.
          condition = ${lookup{$acl_c0}lsearch{/etc/exim4/passwd}{yes}{no}}
          ratelimit = $acl_m1 / 1h / per_rcpt / strict / $acl_c0
  deny    message = Rate limit exceeded: too many messages in 24 hours. Try again later.
          condition = ${lookup{$acl_c0}lsearch{/etc/exim4/passwd}{yes}{no}}
          ratelimit = $acl_m2 / 1d / per_rcpt / strict / $acl_c0

  # Allow authenticated users to relay (MAIL ACL ensures From = authenticated_id)
  accept  authenticated = *
  deny    message = Please log in with a registered account to send mail.
          domains = ! +local_domains
          !authenticated = *
  require message = relay not permitted
          domains = +local_domains : +relay_to_domains
  accept

# Begin routers
begin routers

# Save outbound copies to sender's Sent folder (Roundcube/WHMCS/SMTP clients).
copy_sent:
  driver = redirect
  condition = ${lookup{$sender_address}lsearch{/etc/exim4/passwd}{yes}{no}}
  condition = ${if !eq{$local_part@$domain}{$sender_address}{yes}{no}}
  data = $sender_address
  unseen

sent_copy_delivery:
  driver = accept
  domains = +local_domains
  condition = ${if eq{$local_part@$domain}{$sender_address}{yes}{no}}
  condition = ${lookup{$sender_address}lsearch{/etc/exim4/passwd}{yes}{no}}
  headers_add = X-Save-Copy: sent
  transport = dovecot_lmtp

dnslookup:
  driver = dnslookup
  domains = ! +local_domains
  transport = remote_smtp
  ignore_target_hosts = 0.0.0.0 : 127.0.0.0/8
  no_more

system_aliases:
  driver = redirect
  allow_fail
  allow_defer
  data = ${lookup{$local_part}lsearch{/etc/aliases}}

localuser:
  driver = accept
  senders = lsearch{/etc/exim4/passwd}
  transport = local_delivery
  cannot_route_message = Unknown user

# Begin transports
begin transports

dovecot_lmtp:
  driver = smtp
  protocol = lmtp
  hosts = dovecot-mailserver
  port = 2525
  delivery_date_add
  envelope_to_add
  return_path_add

remote_smtp:
  driver = smtp
  dkim_domain = $sender_address_domain
  dkim_selector = @@DKIM_SELECTOR@@
  dkim_private_key = ${lookup{$sender_address_domain}lsearch{/etc/exim4/dynamic/dkim_paths}}
  dkim_canon = relaxed
  dkim_strict = 0
  # TLS will use default secure ciphers (invalid cipher string removed)

local_delivery:
  driver = appendfile
  directory = /var/mail/vhosts/$domain/$local_part
  maildir_format
  delivery_date_add
  envelope_to_add
  return_path_add
  user = mail
  group = mail

# Begin retry
begin retry

*                      *           F,2h,15m; G,16h,1h,1.5; F,4d,6h

# Begin rewrite
begin rewrite

# Begin authenticators
begin authenticators

plain:
  driver = plaintext
  public_name = PLAIN
  server_condition = "${if crypteq{$auth3}{${extract{1}{:}{${lookup{$auth2}lsearch{/etc/exim4/passwd}}}}}{1}{0}}"
  server_set_id = $auth2
  server_prompts = :
  server_advertise_condition = ${if eq{$tls_cipher}{}{no}{yes}}

login:
  driver = plaintext
  public_name = LOGIN
  server_condition = "${if crypteq{$auth2}{${extract{1}{:}{${lookup{$auth1}lsearch{/etc/exim4/passwd}}}}}{1}{0}}"
  server_set_id = $auth1
  server_prompts = Username:: : Password::
  server_advertise_condition = ${if eq{$tls_cipher}{}{no}{yes}}
